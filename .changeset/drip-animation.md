---
"react-zero-skeleton": minor
---

Add `drip` animation: a vertical shimmer that sweeps top-to-bottom with a subtle skew. Works on web (CSS keyframes) and React Native (Animated + LinearGradient).
